// Description: This file contains all the constants related to caching

// This is the time to live for the cache(in milliseconds)
export enum TIME_IN_MS {
  SECOND = 1000,
  MINUTE = 1000 * 60,
  HOUR = 1000 * 60 * 60,
  DAY = 1000 * 60 * 60 * 24,
  WEEK = 1000 * 60 * 60 * 24 * 7,
  MONTH = 1000 * 60 * 60 * 24 * 30,
  YEAR = 1000 * 60 * 60 * 24 * 365,
}

// This is the key used to store the data in the cache
export enum CACHE_KEYS_PREFIX {
  
}

// This is the time to live for the cache(in milliseconds)
export enum CACHE_TTL {
 
}
